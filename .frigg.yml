tasks:
  - tox -e tests
  - tox -e isort
  - tox -e flake8
  - coverage report --fail-under=60
  - coverage xml

verbose_tasks:
  - coverage report --fail-under=60

comment: false

services:
  - postgresql
  - redis-server

coverage:
  path: coverage.xml
  parser: python
